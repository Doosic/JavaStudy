* http 파일을 생성하여 요청을 보내기 (IntelliJ 유료버전만 사용가능. 무료버전이라면 postman 사용하기)

1. test 파일 및에 http 파일 생성
2. http 파일 오른쪽 상단에 Examples 를 클릭하면 사용법에 대한 자세한 방법이 나와있다.

        - Controller 에 요청 보내기
          Create 이기 때문에 POST 방식으로 보낸다.
          (create(생성 및 데이터 변경) = post(PostMapping), getData(데이터호출) = get(GetMapping)
                                      , edit(데이터 수정) = put(PutMapping), delete(데이터 삭제) = delete(DeleteMapping))

          POST https://localhost:8080/create-monster
          Content-Type: application/json